export const eq = (a, b) => a === b;

/* json para comprobar datos y comprobarlo con aquellos que van a ala vista */
/* export const json = function (context) {
    return JSON.stringify(context);
}; */